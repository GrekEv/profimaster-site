# Инструкция по подключению сайта для домена pfm.by

## ✅ Шаг 1: NS-записи уже настроены

NS-записи уже прописаны:
- **NS1:** `ns1.hostfly.by`
- **NS2:** `ns2.hostfly.by`

---

## 🔧 Шаг 2: Настройка DNS-записей в панели HostFly

**Важно:** Для добавления DNS-записей (A, CNAME) нужно зайти в **cPanel**, а не в клиентскую панель HostFly!

### Способ 1: Через клиентскую панель HostFly

1. Войдите в [панель управления HostFly](https://my.hostfly.by/clientarea.php)
2. В левой панели найдите раздел **"Избранное" (Favorites)**
3. Нажмите **"Вход в cPanel"** (Login to cPanel)
4. В cPanel найдите раздел **"Домены" (Domains)**
5. Откройте **"Редактор зоны" (Zone Editor)**
6. Выберите домен **pfm.by**

### Способ 2: Прямой вход в cPanel

1. Перейдите по адресу: `https://pfm.by:2083` (или используйте IP-адрес вашего сервера)
2. Войдите с теми же учетными данными, что и в клиентскую панель
3. Найдите раздел **"Домены" (Domains)**
4. Откройте **"Редактор зоны" (Zone Editor)**
5. Выберите домен **pfm.by**

### Как добавить DNS-записи в Редакторе зоны:

В **Редакторе зоны** вы увидите список доменов. Для домена **pfm.by** есть кнопки справа:
- `+ A Record` - для добавления A-записи
- `+ CNAME Record` - для добавления CNAME-записи
- `+ MX Record` - для почтовых записей
- `Управлять` - для просмотра и редактирования всех записей

#### Шаг 1: Добавьте A-запись для основного домена

1. Нажмите кнопку **`+ A Record`** рядом с доменом **pfm.by**
2. В открывшемся окне заполните:
   - **Имя (Name):** введите `@` или оставьте пустым (система автоматически подставит домен)
   - **Адрес (Address):** введите `194.62.19.246`
   - **TTL:** оставьте значение по умолчанию (обычно 3600)
3. Нажмите **"Add A Record"** (Добавить A-запись)

#### Шаг 2: Добавьте CNAME для www-поддомена

1. Нажмите кнопку **`+ CNAME Record`** рядом с доменом **pfm.by**
2. В открывшемся окне заполните:
   - **Имя (Name):** должно быть уже заполнено как `www.pfm.by.` (если нет - введите `www`)
   - **CNAME:** замените `example.com` на `pfm.by` (или `pfm.by.` с точкой в конце)
   - **TTL:** оставьте значение по умолчанию
3. Нажмите **"Add A CNAME Record"** (Добавить CNAME-запись)

**Важно:** 
- Если в поле "Имя" уже указано `www.pfm.by.` - это правильно, оставьте как есть
- В поле "CNAME" должно быть `pfm.by` (без `www` и без `http://`)

**Примечание:** IP-адрес `194.62.19.246` - это ваш выделенный IP-адрес. Если IP отличается, проверьте его в cPanel в разделе "Основная информация" → "Dedicated IP Address", или в клиентской панели HostFly в разделе "Аддоны и расширения" → "Выделенный IPv4-адрес".

**Важно:** Если A-запись для `@` уже существует, проверьте, что она указывает на правильный IP-адрес. Если нет - добавьте новую или отредактируйте существующую.

---

## 📁 Шаг 3: Загрузка файлов сайта на хостинг

**Важно:** Загрузка файлов также выполняется через **cPanel**!

### Вариант 1: Через Менеджер файлов (File Manager) в cPanel

1. Войдите в **cPanel** (см. Шаг 2, как зайти в cPanel)
2. Найдите раздел **"Файлы" (Files)**
3. Откройте **"Менеджер файлов" (File Manager)**
4. Перейдите в папку `public_html` (это корневая папка для вашего сайта)
4. Загрузите все файлы вашего сайта:
   - `index.html`
   - `style.css`
   - `script.js`
   - `logo-profimaster.svg`
   - `product-1.png`, `product-2.png`
   - `catalog-profimaster.pdf`
   - `0_Paint_Painting_3840x2160 (1).mp4`
   - и все остальные файлы проекта

### Вариант 2: Через FTP

1. Войдите в **cPanel**
2. Найдите раздел **"Файлы" (Files)**
3. Откройте **"Учетные записи FTP" (FTP Accounts)**
3. Создайте FTP-учетную запись (если еще не создана)
4. Используйте FTP-клиент (FileZilla, WinSCP и т.д.) для загрузки файлов:
   - **Хост:** `ftp.pfm.by` или IP-адрес сервера
   - **Пользователь:** ваш FTP-логин
   - **Пароль:** ваш FTP-пароль
   - **Порт:** 21
5. Загрузите все файлы в папку `public_html`

---

## 🔒 Шаг 4: Настройка SSL-сертификата

### Вариант 1: Бесплатный SSL через AutoSSL (рекомендуется)

1. Войдите в **cPanel**
2. Найдите раздел **"Безопасность" (Security)**
3. Откройте **"SSL/TLS Status"** или **"AutoSSL"**
4. Найдите домен **pfm.by** в списке
5. Если сертификат еще не установлен, нажмите **"Run AutoSSL"** или дождитесь автоматической установки (обычно происходит автоматически в течение 24 часов)

**Примечание:** AutoSSL автоматически устанавливает бесплатные Let's Encrypt сертификаты для всех доменов на аккаунте.

### Вариант 2: Установка через SSL/TLS Wizard (платные сертификаты)

Если AutoSSL недоступен или вы хотите установить платный сертификат:

1. Войдите в **cPanel**
2. Найдите раздел **"Безопасность" (Security)**
3. Откройте **"SSL/TLS Wizard"**
4. Вы увидите список доменов и поддоменов

#### Какие домены выбрать для SSL-сертификата:

**Обязательно выберите:**
- ✅ **`pfm.by`** - основной домен вашего сайта (уже отмечен на вашем скриншоте)

**Рекомендуется также выбрать (если планируете использовать):**
- ✅ **`mail.pfm.by`** - для почтового сервера
- ✅ **`webmail.pfm.by`** - для веб-интерфейса почты

5. Нажмите **"Продолжить" (Continue)**
6. **Внимание:** На следующем шаге вам предложат выбрать платный сертификат ($9-$108)

**Рекомендация:** Если вы видите только платные варианты, лучше использовать **AutoSSL** (Вариант 1) для бесплатного Let's Encrypt сертификата.

### После установки SSL:

1. Включите принудительное перенаправление на HTTPS:
   - В cPanel → **"Безопасность"** → **"SSL/TLS"** → **"Force HTTPS Redirect"**
   - Или добавьте правило в `.htaccess` файл в папке `public_html`
2. Проверьте работу: `https://pfm.by`

---

## 📧 Шаг 5: Настройка почты (если необходимо)

Если нужна почта на домене pfm.by:

1. Войдите в **cPanel**
2. Найдите раздел **"Почта" (Mail)**
3. Откройте **"Почтовые учетные записи" (Mail Accounts)**
3. Создайте почтовые ящики (например, `info@pfm.by`, `sales@pfm.by`)
4. MX-записи обычно настраиваются автоматически, но проверьте в **"Редактор зоны"**

---

## ✅ Шаг 6: Проверка работы сайта

После загрузки файлов и настройки DNS:

### Проверка DNS-записей через терминал:

```bash
# Проверка A-записи для основного домена
dig pfm.by A +short
# Должен вернуть: 194.62.19.246

# Проверка CNAME для www-поддомена
dig www.pfm.by CNAME +short
# Должен вернуть: pfm.by.

# Проверка NS-записей
dig pfm.by NS +short
# Должен вернуть: ns1.hostfly.by. и ns2.hostfly.by.

# Проверка доступности сайта
curl -I http://pfm.by
# Должен вернуть: HTTP/1.1 200 OK
```

### Проверка сайта в браузере:

- Откройте в браузере: `http://pfm.by`
- Откройте: `http://www.pfm.by`
- После установки SSL: `https://pfm.by`

### Онлайн-инструменты для проверки:

- [whatsmydns.net](https://www.whatsmydns.net/#A/pfm.by)
- [dnschecker.org](https://dnschecker.org/#A/pfm.by)

---

## 🎯 Краткая инструкция (быстрый старт)

1. ✅ NS-записи уже настроены
2. 🔧 **Войдите в cPanel:**
   - Через клиентскую панель HostFly → **"Избранное"** → **"Вход в cPanel"**
   - Или напрямую: `https://pfm.by:2083`
3. 🔧 **В cPanel:** **Домены** → **Редактор зоны** → выберите `pfm.by` → добавьте:
   - **A-запись:** `@` → `194.62.19.246`
   - **CNAME:** `www` → `pfm.by`
4. 📁 **Загрузите файлы:** В cPanel → **Файлы** → **Менеджер файлов** → папка `public_html`
5. 🔒 **Установите SSL:** В cPanel → **Безопасность** → **SSL/TLS Wizard**
6. ✅ **Проверьте работу сайта:** `http://pfm.by` и `https://pfm.by`

---

## ⚠️ Важные моменты

- ⏱️ **Время распространения DNS:** Обычно 5-30 минут, но может занять до 24 часов
- 🔄 **Кэш DNS:** Если сайт не открывается, очистите кэш DNS:
  - Windows: `ipconfig /flushdns`
  - macOS/Linux: `sudo dscacheutil -flushcache` или `sudo systemd-resolve --flush-caches`
- 📁 **Структура файлов:** Убедитесь, что `index.html` находится в папке `public_html`
- 🔒 **HTTPS:** После установки SSL-сертификата сайт будет доступен по `https://pfm.by`

---

## 📞 Контакты поддержки HostFly

Если возникнут проблемы:
- **Телефон:** +375 17 336-73-73
- **Email:** sales@hostfly.by
- **Панель управления:** [my.hostfly.by](https://my.hostfly.by/clientarea.php)
- **Круглосуточная поддержка:** +375 17 336-73-73

---

## 📝 Структура файлов для загрузки

Убедитесь, что загрузили все эти файлы в `public_html`:

```
public_html/
├── index.html
├── style.css
├── script.js
├── logo-profimaster.svg
├── logo-profimaster.png
├── product-1.png
├── product-1.jpg
├── product-1.svg
├── product-2.png
├── product-2.jpg
├── product-2.svg
├── catalog-profimaster.pdf
├── 0_Paint_Painting_3840x2160 (1).mp4
└── hero-video.mov (если используется)
```
